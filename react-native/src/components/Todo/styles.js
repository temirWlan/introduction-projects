import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
	todo: {
		marginTop: 10,
		padding: 15,
		flexDirection: 'row',
		backgroundColor: '#fff',
		borderWidth: 1,
		borderStyle: 'solid',
		borderColor: '#777',
		borderRadius: 5
	}
});

export default styles;